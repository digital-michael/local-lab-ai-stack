{
  "_comment": "Model routing definitions for LiteLLM. Each entry in default_models is registered via scripts/pull-models.sh. The 'id' field must match the model_name registered in LiteLLM so it appears in /models responses.",
  "default_models": [
    {
      "id": "llama3.1-8b",
      "description": "Llama 3.1 8B GGUF — primary local inference model, served by llamacpp",
      "litellm_params": {
        "model": "openai/llama3.1-8b",
        "api_base": "http://llamacpp:8080/v1",
        "api_key": "none",
        "max_tokens": 4096
      },
      "model_info": {
        "mode": "chat",
        "input_cost_per_token": 0,
        "output_cost_per_token": 0
      }
    }
  ]
}
