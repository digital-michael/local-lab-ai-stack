# Project Review Log — llm-agent-local-2
**Last Updated:** 2026-05-04 UTC
**Target Audience:** LLM Agents

---

## Purpose

This file captures raw reinforcement workflow review log data for this project. For the review log schema and field definitions, see [../meta/meta_metrics.md](../meta/meta_metrics.md).

---

## Review Log

| Date | Trigger Commit | Interaction Level | Focus Item Applied | Outcome Observed | Decisions Recorded | Lateral Ideas Surfaced | Driver |
|---|---|---|---|---|---|---|---|
| 2026-03-08 | `93c912c` phases 1–5 complete | 4 | none (first cycle) | yes — structured 6-phase execution plan with explicit I/O and verification criteria enabled Phases 1–5 completion in one session; quadlet generation debugged and validated end-to-end | 4 (D-011–D-014) | 0 | human |
| 2026-03-10 | `08874e6` Phase 8d + test clean | 4 | L6 (prior cycle — front-loaded spec) | pytest 16→23 passed, 9→2 skipped; T-072 (tool-calling Modelfile), T-086 (Authentik forward-auth bootstrap + Traefik route fixes), T-062–T-068 (knowledge-index service built from scratch); Phase 6 housekeeping closed; 2 skips remain (T-071 hardware-gated, T-066 Flowise manual setup) | 0 | 0 | human |
| 2026-03-12 | `e3d9a16` T-066 Flowise unblock | 4 | — | pytest 23→24 passed, 2→1 skipped; T-066 unblocked via Flowise 3.x API: admin registration, API key, Qdrant credential, RAG chatflow (conversationalRetrievalQAChain+chatOllama+qdrant+ollamaEmbedding); key learnings: password policy, /api/v1/auth/login endpoint, chatflow type field, full inputParams/inputAnchors required in flowData | 0 | 0 | human |
| 2026-03-22 | `b2c233e` Phase 9d verified | 4 | L-5 (separation of concerns applied to topology) | Phase 10 topology redesign: `knowledge-worker` profile introduced; controller-as-custodian (D-022/D-023 revised); library-as-asset framing adopted; SQLite for workers confirmed zero-cost catch; D-025 establishes provenance/lifecycle foundation for future licensing | 4 (D-018r, D-022r, D-023r, D-024, D-025) | 1 (L-9) | human |
| 2026-03-24 | `27efd06` Phase 12.8 complete | 4 | — | Phase 12 complete (all controller-side): Traefik MCP route split with Authentik auth (D-030); MinIO service added (configs/quadlets + configs/minio); capabilities[] field on node configs (D-029); LiteLLM AsyncRAGHook for inference+knowledge-index (D-030); POST /v1/search endpoint + SQLite library custody in app.py (D-031 Phase A); diagnose.sh _check_ki_capabilities + /v1/catalog auth fix; Flowise research-pipeline.json Tool Agent flow (D-031 Phase A); configure.sh enhanced-worker profile + schema 1.2 validation (D-029); knowledge-worker retained as legacy alias throughout. Phase 13 defined: live deployment verification + L5 distributed test execution. | 0 | 0 | human |
| 2026-04-04 | `061f806` Phase 23 complete | 3 | — | Phase 22 (Dynamic Node Registration) verified complete in live deployment: TC25 (macOS) and SOL (Linux) joined, heartbeating, `online`. Phase 23 delivered: bootstrap.sh hardened (OnCalendar timer, heredoc .service unit, HEARTBEAT_SCRIPT guard, macOS launchd bootstrap/gui domain); sleep inhibitor (scripts/inhibit.sh) — caffeinate (macOS) + systemd-inhibit idle-block (Linux), opt-in via sleep_inhibit in config.json, wired into start.sh/stop.sh. operator-faq.md updated with node lifecycle and sleep inhibitor how-to. features.md updated: Dynamic Node Registration and Security Audit Tool promoted to Core Features `[X]`; Worker Sleep Inhibitor added. | 0 | 0 | human |
| 2026-04-05 | `550976e` P1 fixes | 3 | — | P1-1: AsyncRAGHook not loading in LiteLLM 1.81.14 — root causes (a) callbacks under general_settings instead of litellm_settings, (b) get_instance_fn returns class not instance — fixed via module-level singleton + config section move. P1-1b: qwen3:8b thinking mode exhaust max_tokens — fixed via reasoning_effort=none injection in hook (maps to think:false in ollama_chat provider). P1-2: Ollama port restricted to 127.0.0.1 on controller; FAQ updated with bare-metal worker firewall instructions. 2 commits: fc90a26, 550976e. | 0 | 0 | human |
| 2026-04-06 | `a656653` alias-in-DB | 2 | — | Architectural fix for node_id/JSON disconnect. Added alias TEXT column to nodes DB (migration); JoinRequest+HeartbeatRequest accept alias; join stores it, heartbeat backfills it for pre-upgrade nodes; list_nodes+get_node return it. heartbeat.sh: one-time alias derivation from configs/nodes/ by node_id match, saves to state. node.sh: join --alias flag; list now matches by alias (primary) + node_id fallback; rename JSON sync block removed; harden-worker --alias flag added, --node-id kept as backward compat. | 0 | 0 | human |
| 2026-04-08 | `9d33dce` Phase 24 / traefik fixes | 2 | — | Phase 24 (BL-009 Phase 1): ContentReviewHook added to hooks.py with Category A/B/C pattern sets; registered before AsyncRAGHook; _review_content() added to app.py at both ingestion endpoints; T-REV-001–012 tests (9/9 unit passing); pytest.ini requires_ki mark registered. Traefik/TLS housekeeping: deployed tls.yaml was referencing stale cert.pem (CN=localhost) instead of server.crt (AI Stack Local CA); services.yaml deployed copy was also stale (missing Host constraints on KI routes). Both deployed configs synced. Added ki.stack.localhost Traefik router for /admin path (API-key-only, no Authentik); added Host('ki.stack.localhost') constraint to existing /mcp and /v1 KI routes to close path-only match gap. status.sh _svc_url updated for knowledge-index. | 0 | 0 | human |
| 2026-05-04 | `a0b0e6f` status.sh + architecture decisions | 3 | — | status.sh: tailnet row added (`_tailnet_status()` via `tailscale status --json`; shows BackendState, self IP, online/total peers); m2m-gateway excluded via `_SKIP_SVCS` (reversible, TODO commented). Architecture L3/L4 session: D-004–D-008 recorded in `docs/meta_local/decisions.md`. Decisions: headscale as authoritative presence (D-004); distributed node config pull model — deprecates `configs/nodes/*.json` workers (D-005); pull-based metrics via node-exporter-ai sidecar binding to tailnet IP (D-006); tailscale SSH as transitional CNC transport with ACL hardening + non-SSH target state (D-007); LAN→tailnet IP migration strategy controller-first (D-008). plan.md rewritten: BL-011–BL-016 added (headscale migration, distributed config, node-exporter-ai, node.sh remote, non-SSH CNC, Headplane). WIP doc audit: headscale-proposal/install/headplane-remote-deploy KEEP; m2m-* PARTIAL (parallel track). Two pushbacks accepted: (1) headscale VPS SPOF — three-tier fallback (LAN SSH / local headscale / LAN fallback) planned; (2) SSH ACL blast radius — directional ACL tightening is BL-011 step 1 before any command delivery use. | 5 (D-004–D-008) | 0 | human |
| 2026-05-04b | `d904c1d` flush-to-disk + BL-011 p1 | 2 | — | BL-011 step 1: directional headscale ACL deployed (tag:controller→tag:inference,knowledge SSH only); ghost node 1 deleted; node 5 retagged with tag:controller,tag:knowledge,tag:net-ecotone-000-01; `tailscale ssh sol` verified exit 0. BL-011 step 2: §7.11 LAN SSH break-glass added to CENTAURI-playbook.md (LAN IPs, SSH user, key for all nodes). Steps 3+4 blocked: port 8100 is `bind: 127.0.0.1`; workers cannot reach tailnet IP; `controller_url` reverted to LAN hostname; BL-015 must ship first. Flush-to-Disk session protocol added to `.github/copilot-instructions.md`: narrow flush (immediate, silent) + broad flush (before every commit, announced). Lessons written: port-binding blocker, ghost node / dual-enrollment issue (headscale). | 0 | 0 | human |
| 2026-05-05 | `48810c7` BL-012 complete | 2 | — | BL-012 (Distributed Node Config) complete. `node.sh configure` writes `~/.config/ai-stack/node-config.json` (schema 1.2): node_id, alias, profile, os, deployment, capabilities, models (Ollama API), updated_at. Profile resolution: state file → `configs/nodes/` static lookup by alias/node_id → default. `node.sh list --refresh` pulls `node-config.json` from each online headscale node via `tailscale ssh`; self-node uses local copy. Cache at `~/.config/ai-stack/nodes/`. List display reads cache first (priority over static files), staleness warning >10 min. Verified on all nodes: CENTAURI (controller, 9 models), SOL (inference-worker, linux, bare_metal, Ollama not running), TC25 (inference-worker, darwin, bare_metal, llama3.1:8b-instruct-q4_K_M). Known gap: `tailscale ssh` host key rejected on headscale self-hosted setups; TC25 refresh fell through to static file; workaround is direct SSH via tailnet IP with key pre-installed. Architecture §7 Node Management Layers table added. Lessons written: headscale SSH host key issue, givenName vs HostName mismatch. | 0 | 0 | human |
| 2026-05-05b | pending BL-015 implementation | 2 | — | BL-015 (Tailnet-Accessible KI Endpoint + CNC Foundation) implemented and verified. Traefik `tailnet` entrypoint added (`:8443`, binds `100.64.0.4:8443:8443` at host); services.yaml `knowledge-index-tailnet-api` + `knowledge-index-tailnet-mcp` routers added. Knowledge-index: `CNC_BEARER_TOKEN` env var, `GET /v1/config` (public bootstrap — no auth), `POST /v1/cnc/register` + `POST /v1/cnc/heartbeat` (bearer token). config.json: `tailnet` block, `network_domains`, `TAILNET_CONTROLLER_URL` + `TAILNET_DOMAIN` env vars for KI container, `knowledge_index_cnc_token` Podman secret reference. `node.sh configure` now resolves `controller_url` (flag → config.json → GET /v1/config discovery) and writes `network.controller_url` + `network.bearer_token` into node-config.json. heartbeat.sh migrated: reads `$STATE_DIR/network_bearer_token`, uses `/v1/cnc/heartbeat` when bearer token present, falls back to legacy LAN endpoint otherwise. Podman secret `knowledge_index_cnc_token` created. capture-credentials.sh updated. Lesson: Traefik inside container cannot bind host tailnet IP — use `:8443` (wildcard) in traefik.yaml; host restriction is via `PublishPort=100.64.0.4:8443:8443`. Verification gate: all 5 checks passed. BL-011 steps 3+4 now unblocked. | 1 (lesson: Traefik container IP binding) | 0 | human |
| 2026-05-05c | pending BL-011 worker migration | 2 | — | BL-011 steps 3+4 complete. SOL: state files written, CNC heartbeats confirmed streaming every 30s. TC25 went offline after state file provisioning; came back, git pulled to `5a13f15`, 2 consecutive CNC heartbeats triggered — TC25 status `online`. Bug found and fixed: `/v1/cnc/heartbeat` handler in app.py was missing `_nr._transition_up()` call after heartbeat insert — `caution`/`failed` nodes could not recover to `online` via CNC path; also had wrong inline guard (promoted from terminal states instead). Fix: remove inline guard; call `_nr._transition_up(req.node_id, conn)` after `conn.commit()`. KI rebuilt and redeployed (image `493123416048`, tag `0.1.0`). §7.12 added to CENTAURI-playbook.md: CNC bearer token provisioning procedure (pexpect pattern for TC25, SOL SSH one-liner, verification). Lesson written. BL-011 ✅ done. | 1 (lesson: cnc_heartbeat missing _transition_up) | 0 | human |
| 2026-05-05d | pending BL-009 Ph.2 | 2 | — | BL-009 Phase 2 (guard LLM, Category D) implemented. `_guard_d_check` async function added to `configs/litellm/hooks.py`: reads `REVIEW_GUARD_MODEL` / `REVIEW_GUARD_URL` / `REVIEW_D_FAIL_MODE` at call time; makes a stateless context-isolated Ollama `/api/chat` call with `temperature=0` and 20 token budget; parses `SAFE \| UNSAFE:<category>` response; raises ValueError on UNSAFE; on error logs and allows if `fail_mode=open` (default), rejects if `closed`. `ContentReviewHook.async_pre_call_hook` now awaits `_guard_d_check` after regex gates. Equivalent sync `_ki_guard_d_check` added to `services/knowledge-index/app.py`; called from `_review_content` after A/B/C regex gates. `proxy_config.yaml` comments updated. T-REV-013–018 added to `testing/layer3_model/test_content_review.py`: all 15 unit tests pass (9 Phase 1 + 6 Phase 2). Env: guard disabled by default (REVIEW_GUARD_MODEL unset = Category D skipped). | 0 | 0 | human |
| 2026-05-05e | execute BL-015 step 9 | 1 | — | BL-015 step 9 completed: removed LAN hostname `SERVICES.mynetworksettings.com` from `configs/traefik/dynamic/services.yaml` (`knowledge-index-admin` router now `Host(ki.stack.localhost) && PathPrefix(/admin)` only). This finalizes BL-015 LAN cleanup after worker tailnet stability verification. Plan queue updated to mark BL-015 step 9 done. | 0 | 0 | human |
| 2026-05-06b | pending BL-016 | 2 | — | BL-016 (Headplane deployment on photondatum.space) complete. Headplane v0.6.2 deployed as Podman quadlet (`/etc/containers/systemd/headplane.container`). Config at `/etc/headplane/config.yaml` (chmod 600): bound to tailnet IP `100.64.0.5:3000`, headscale at `http://127.0.0.1:8080`, full-feature mode via `/etc/headscale/config.yaml` bind mount. Two bugs hit: (1) `kubernetes.pod_name` required even when kubernetes disabled — fixed by adding `pod_name: ""` to config; (2) `systemctl enable` fails for generated quadlet units — use `systemctl start` only. Firewall: `tailscale0` assigned to `trusted` zone; port 3000 not in `public` zone. Verification: tailnet 302, public internet connection refused. | 3 (headplane schema bug, quadlet enable pattern, firewalld tailscale zone) | 0 | human |
| 2026-05-06 | pending BL-014 | 2 | — | BL-014 (node.sh remote: SSH command delivery wrapper) implemented. `cmd_remote` added to `scripts/node.sh`: resolves node alias/node_id to tailnet IP via `tailscale status --json` (Python3 inline), SSHes with `StrictHostKeyChecking=no UserKnownHostsFile=/dev/null ConnectTimeout=10` (direct SSH — `tailscale ssh` rejected by headscale, no host keys served). SSH exit 255 = connection error → falls back to LAN IP (`address_fallback` + `ssh_user` from `configs/nodes/*.json`). `ssh_user` field added to `inference-worker-1.json` (TC25: michaelbiggerstaff) and `inference-worker-2.json` (SOL: 3pdx7). Header comment, `usage()`, and main dispatch updated. Step 3 (suggestions migration) deferred — requires workers to cache suggestions locally first. Verification: `node.sh remote sol echo BL-014` → `BL-014`, exit 0. Note: SOL repo path is `~/Projects/active/local-lab-ai-stack/`, not `~/ai-stack/scripts/` (heartbeat quadlet path was for data dir). | 1 (lesson: tailscale ssh rejected on headscale; direct SSH required; SOL repo path) | 0 | human |
| 2026-05-06c | pending BL-008 | 2 | — | BL-008 (Default credential policy) implemented as Check F in `configure.sh security-audit`. Check F reads each Podman secret listed in `config.json` (`.services[].secrets[].name`), skips provider API keys (openai/anthropic/groq/mistral), flags CRITICAL for known-weak values (changeme, password, admin, etc.), WARNING for length < 12 or secret missing from store. Skips gracefully if `podman` unavailable. Bonus fix: Check D false positive fixed — `.name` and `.target` metadata fields now excluded from plaintext secret scan. T-117/T-118/T-119 bats tests added (fake `podman` stub via PATH override). All 7 layer0_security_audit tests pass. | 1 (lesson: Check D false positive on secrets[].name and .target fields) | 0 | human |
| 2026-05-06d | no commit — operational fixes | 1 | — | **LiteLLM /metrics:** `prometheus` callback added to `configs/litellm/proxy_config.yaml`; deployed copy of `hooks.py` fixed — deployed `~/ai-stack/configs/litellm/hooks.py` had drifted from repo (lacked `content_review_hook = ContentReviewHook()` module-level singleton added in BL-009 Ph.2). Service was running from cached state; clean restart after `prometheus` callback addition exposed the drift. Fix: copy repo source to deployed path. **Homepage dashboard widget fixes (5 errors):** All root-cause traced to missing `HOMEPAGE_VAR_*` env vars in `homepage.container` quadlet (vars not generated by `configure.sh`). Fixed: `HOMEPAGE_VAR_GRAFANA_USER/PASS=admin/admin`, `HOMEPAGE_VAR_QDRANT_API_KEY=qdrant-test-api-key`, `HOMEPAGE_VAR_AUTHENTIK_TOKEN=<akadmin API token>` added to `~/.config/containers/systemd/homepage.container`; `knowledge-index.service` started (was inactive). Authentik widget JS crash traced to unauthenticated API call causing `pagination` to be undefined. **Playbook:** §10.6 performance reference added (LiteLLM Prometheus metrics, Ollama GPU spot checks, Qdrant, Traefik, host resources); URL tables updated (Homepage first); §11 homepage widget troubleshooting entry added. **Docs:** `getting-started.md` troubleshooting section added. | 2 (lessons: hooks.py deployment drift; HOMEPAGE_VAR_* not persistent) | 0 | human |

## Pending Tasks / Backlog

_See `docs/wip/plan.md` for the full prioritized backlog with step-by-step implementation plans. Summary below._

| ID | Priority | Title | Status |
|---|---|---|---|
| BL-011 | P1 | Headscale migration: ACL hardening + LAN break-glass + IP migration | ✅ done |
| BL-015 | P1 | Tailnet-accessible KI endpoint + CNC foundation (+ LAN cleanup step 9) | ✅ done |
| BL-012 | P1 | Distributed node config: `node.sh configure` + `--refresh` | ✅ done |
| BL-009 | P1 | Content Review Layer Phase 2 (guard LLM) | ✅ Phase 2 done — pending commit |
| BL-013 | P2 | node-exporter-ai: per-node Prometheus metrics exporter | not started |
| BL-014 | P2 | node.sh remote: SSH command delivery wrapper | ✅ done |
| BL-016 | P2 | Headplane deployment on photondatum.space | ✅ done |
| BL-016 | P2 | Headplane deployment on photondatum.space | not started |
| BL-008 | P2 | Default credential policy | ✅ done |
| BL-004 | P2 | RLM integration research | not started |
| BL-005 | P2 | Internal operator dashboard | not started |
| BL-006 | P2 | Live throughput + profiling dashboard | not started |
| BL-007 | P2 | Configurable domain in setup | not started |
| BL-010 | P3 | Evaluate peer/node registration architecture | deferred |
